# 基石 · 本地化批量文档脱敏工具 · 实施落地文档

> 产品定名：**基石**（M1 开发中，本文档为实施依据）

> **文档用途**：本文档是产品的完整实施依据，供开发者或执行型 AI 模型直接按步骤落地。
> **使用方法**：从第 2 章环境准备开始，按 M1 → M2 → M3 阶段顺序执行；每个任务末尾有【自查】块，全部通过后才能进入下一任务；遇到本文档未覆盖的决策点，先查第 9 章"开放问题"，仍无答案则暂停并向产品负责人确认，不要自行假设。
> **配套文档**：产品方案（功能规则、界面原型、指标定义）以本文档第 1、4、5 章为准，本文档已内联全部必要规则，可独立使用。

---

## 1. 产品定稿（不可变更的核心约定）

实现过程中以下约定不得偏离，如有冲突以本节为准：

1. 纯本地离线运行，运行时禁止任何网络请求；所有模型、词典随安装包分发。
2. 支持 `.docx`、`.pdf`（文字型）批量脱敏；扫描件 PDF 检测后标注"暂不支持"。
3. 同批次内同一原文的代称全局唯一、跨文档一致。
4. 所有自动识别结果必须可在界面上人工启停，未确认前低置信度结果不生效。
5. 脱敏不覆盖源文件，默认输出到源目录下 `脱敏输出/` 子目录。
6. 导出时同步生成映射 JSON，默认密码加密（AES-256-GCM + scrypt）。
7. 回溯模式凭"AI处理后文档 + 映射文件 + 密码"还原；还原后必须产出"未还原项"报告。
8. 代称词表：天干 `甲乙丙丁戊己庚辛壬癸`，用尽量地支 `子丑寅卯辰巳午未申酉戌亥`，再用尽则追加序号（甲某2、乙某2……）。人名后缀"某"，公司后缀"公司"，单位后缀"单位"，地址后缀"地址"。

### 1.1 脱敏规则定稿

| 类别 | 识别方式 | 默认脱敏规则 | 可配置项 |
|---|---|---|---|
| 身份证号 | 正则 + GB11643 校验位验证 | 保留前3后4，中间 `*` 掩码 | 首尾保留位数 |
| 手机号 | 正则 `1[3-9]\d{9}` | 保留前3后4：`138****5678` | 同上 |
| 银行卡/账号 | 正则 + Luhn 校验 | 保留前4后4 | 同上 |
| 护照/通行证/驾驶证/社保/公积金 | 各格式正则 | 保留前2后2 | 同上 |
| 电子邮箱 | 正则 | 保留首字符，其余掩码：`z***@***` | 域名是否保留 |
| 微信号 | 正则（字母开头6~20位）+ 上下文（"微信号"等提示词） | 全掩码 | — |
| 人名 | 上下文规则 + 姓氏词典 + NER 模型 | 甲某、乙某……序列代称 | 司法人员姓名是否脱敏（默认保留） |
| 公司/单位 | NER(ORG) + 后缀词典（有限公司/事务所/银行等） | 甲公司、乙单位……序列代称 | — |
| 地址 | NER(LOC) + 地址特征词（路/街/号/室） | 甲地址、乙地址……序列代称 | — |
| 统一社会信用代码 | 18位正则 + 校验位 | 保留前2后2 | 同上 |
| 车牌号 | 正则（含新能源） | 保留汉字+首字母，其余掩码：`京A***8` | — |
| 开户行/证券账户 | 上下文正则 | 映射为 甲公司 序列或掩码 | — |
| 出生日期 | 日期正则 + 上下文（"出生"等） | 保留年份：`1985年**月**日` | 是否全掩码 |
| 民族 | 枚举词典（56个民族） | 替换为"某族" | — |

### 1.2 验收指标（最终自查的量化标准）

| 指标 | 门槛 |
|---|---|
| 号码类识别召回率 | ≥ 99% |
| 人名/机构名召回率 / 精确率 | ≥ 95% / ≥ 90% |
| 格式保真 | 导出文档与原文档排版差异 0 处（替换词自身除外） |
| 性能 | 100 页 docx 预扫描 ≤ 10 秒（纯 CPU，目标机实测） |
| 回溯还原 | 映射覆盖的代称 100% 还原 |
| 离线性 | 运行时无任何外联（防火墙全阻断环境下功能完整） |

---

## 2. 技术选型与环境准备

### 2.1 技术栈（已锁定）

| 层 | 选型 | 说明 |
|---|---|---|
| 语言 | Python 3.11 | — |
| 界面 | PySide6 | Qt 原生界面，QThread 做异步 |
| Word 解析 | python-docx | run 级读写 |
| PDF 解析 | PyMuPDF (fitz) | span 级坐标 + redaction |
| NER 推理 | onnxruntime（CPU） | 模型转 ONNX 后本地推理 |
| 加密 | cryptography | AES-256-GCM + scrypt |
| 测试 | pytest | — |
| 打包 | PyInstaller | onedir 模式 |

### 2.2 项目结构（按此创建）

```
desensitizer/
├── app/
│   ├── main.py                    # 入口：QApplication + 主窗口
│   ├── ui/
│   │   ├── main_window.py         # 三栏布局 + 顶栏 + 模式切换
│   │   ├── file_queue.py          # 左栏：文件队列控件
│   │   ├── preview.py             # 中栏：预览控件
│   │   ├── mapping_panel.py       # 右栏：映射面板（启停/新增/搜索/待确认）
│   │   └── dialogs.py             # 自定义脱敏项弹窗、密码弹窗、确认弹窗
│   ├── core/
│   │   ├── pipeline.py            # 批次队列 + 文件状态机
│   │   ├── parser/
│   │   │   ├── base.py            # 解析器协议：返回 TextSpan 列表
│   │   │   ├── docx_parser.py
│   │   │   └── pdf_parser.py
│   │   ├── recognize/
│   │   │   ├── engine.py          # 识别流水线 + 仲裁器
│   │   │   ├── rules.py           # 正则层（含校验位验证）
│   │   │   ├── lexicon.py         # 词典/上下文规则层
│   │   │   └── ner.py             # NER 模型层（M3 接入）
│   │   ├── mapping/
│   │   │   ├── map_table.py       # 全局映射表（批次单例）
│   │   │   ├── alias_gen.py       # 代称生成器
│   │   │   └── crypto.py          # 映射文件加解密
│   │   ├── redact/
│   │   │   ├── docx_redactor.py   # run 拆分替换
│   │   │   └── pdf_redactor.py    # redaction 坐标替换
│   │   └── restore/
│   │       └── restorer.py        # 回溯逆替换 + 未还原项报告
│   └── resources/
│       ├── lexicons/              # 姓氏、民族、司法停用词、提示词等
│       └── models/                # NER ONNX 模型（M3 放入）
├── tests/
│   ├── unit/                      # 单元测试
│   ├── corpus/                    # 评测语料 + 标注 JSON
│   └── eval.py                    # 召回/精确率评测脚本
└── packaging/
    └── build.spec                 # PyInstaller 配置
```

### 2.3 环境准备步骤

1. 安装 Python 3.11（64 位），新建虚拟环境：`python -m venv .venv && .venv\Scripts\activate`
2. 安装依赖：`pip install PySide6 python-docx PyMuPDF cryptography pytest`，M3 追加 `pip install onnxruntime`
3. 初始化 git 仓库，按 2.2 创建目录骨架
4. 准备词典文件放入 `app/resources/lexicons/`：
   - `surnames.txt`：百家姓 + 常见复姓（欧阳、司马、诸葛等）
   - `ethnicities.txt`：56 个民族名称
   - `judicial_stopwords.txt`：人民法院、人民检察院、仲裁委、公安局等（永不识别为人名）
   - `prompt_words.txt`：原告、被告、上诉人、被上诉人、申请人、被申请人、法定代表人、负责人、委托诉讼代理人、委托代理人、证人、第三人、被害人、犯罪嫌疑人、被告人
   - `titles.txt`：职务词表（审判长、审判员、书记员、律师、主任等，配合"司法人员是否脱敏"开关使用）

【自查】
- [ ] `python -c "import PySide6, docx, fitz, cryptography"` 无报错
- [ ] 目录结构与 2.2 一致
- [ ] 四个词典文件齐备且为 UTF-8 编码

---

## 3. M1 阶段：Word 脱敏主链路（可演示的最小闭环）

目标：Word 批量导入 → 规则+词典识别 → 映射面板人工干预 → 脱敏导出 + 明文映射 JSON。本阶段不含 PDF、加密、回溯、NER。

### 任务 M1-1：代称生成器 `alias_gen.py`

实现：
- 序列源：10 天干 → 12 地支 → 天干+序号（甲2、乙2……）
- `next_alias(category: str) -> str`：按类别拼后缀（person→"某"，company→"公司"，org→"单位"，address→"地址"）
- 每类别独立计数；支持 `reserve(alias)`（用户自定义代称占用防冲突）与 `release(alias)`（删除映射时回收）

【自查】`tests/unit/test_alias_gen.py`
- 连续取 23 个人名代称：第 1 个为"甲某"，第 11 个为"子某"，第 23 个为"甲某2"
- reserve("乙某") 后，乙某不再被自动分配
- pytest 全绿

### 任务 M1-2：规则识别层 `rules.py`

实现：每个号码类一个识别函数，返回 `(原文, 类别, 起止位置, 置信度)`：
- 身份证：18 位正则 + 校验位模 11 算法验证，校验不通过则丢弃（防假阳性）
- 手机号：`1[3-9]\d{9}`
- 银行卡：13~19 位数字 + Luhn 校验
- 护照（E/G 开头格式）、港澳通行证（H/M 开头）、驾驶证（身份证号即证号，合并到身份证规则）、社保/公积金（地区格式差异大，先覆盖 9~18 位纯数字+上下文"社保/公积金"）
- 邮箱、统一社会信用代码（18 位 + 校验位）、车牌号（含新能源 8 位）
- 所有规则层命中置信度固定 0.99

【自查】`tests/unit/test_rules.py`
- 每类至少 5 个正样本命中、5 个负样本（校验位错的身份证、12 位手机号等）不命中
- pytest 全绿

### 任务 M1-3：词典/上下文识别层 `lexicon.py`

实现：
- 上下文模式：对 `prompt_words.txt` 每个提示词，匹配其后 `[:：\s]*([一-龥]{2,4})` 且首字命中姓氏表的片段，判为人名，置信度 0.85
- 称谓模式：`([一-龥]{2,4})，(男|女)` 结构判为人名，置信度 0.85
- 公司：含"有限公司/股份有限公司/事务所/银行/集团"结尾的连续片段，置信度 0.8
- 地址：含"省/市/区/路/街/号/室"且长度 ≥ 6 的连续片段，置信度 0.7
- 民族、出生日期按词典与日期正则识别
- 所有命中先经 `judicial_stopwords.txt` 过滤，命中停用词直接丢弃

【自查】`tests/unit/test_lexicon.py`
- "原告张三，男，汉族" → 张三（人名）、汉族（民族）均命中
- "北京市海淀区人民法院" → 不产生任何人名命中
- pytest 全绿

### 任务 M1-4：仲裁器 `engine.py`

实现 `scan(text_spans) -> List[Finding]`：
1. 依次跑规则层、词典层
2. 重叠区间合并：同类别取最长区间；类别冲突时规则层优先
3. 阈值分桶：置信度 ≥ 0.90 → `auto`（直接生效）；0.60~0.90 → `pending`（待确认，不生效）；< 0.60 丢弃
4. 输出去重后的 Finding 列表（原文、类别、位置、置信度、来源）

【自查】
- 同一区间同时被身份证规则和手机规则命中时，保留身份证（类别冲突规则层优先且区间更长）
- 置信度 0.85 的人名进入 pending，不进 auto（注意：这意味着 M1 阶段人名默认进待确认组，属预期行为，M3 模型融合后复议）

### 任务 M1-5：全局映射表 `map_table.py`

实现：
- 以原文为键的字典，值为 `{category, alias, occurrences, enabled, source, confidence}`
- `add_finding(finding)`：新原文自动调 `alias_gen.next_alias()`；已存在则累计次数
- `set_enabled(original, bool)`：启停，不回收代称
- `remove(original)`：删除并回收代称，返回受影响提示
- `add_custom(original, alias=None)`：手工新增，未指定代称则自动分配
- `split_entity(original, file_ids)`：同名主体拆分（按文件粒度拆出独立代称）

【自查】pytest：同原文两次添加代称不变；启停不改变代称；删除后回收可复用；自定义项即时出现在全表

### 任务 M1-6：docx 解析与替换 `docx_parser.py` / `docx_redactor.py`

解析：遍历正文段落 + 所有表格（含嵌套）+ 页眉页脚 + 脚注 + 文本框（`w:txbxContent`），产出段落列表。

替换（核心算法，必须严格按此实现）：
```
对每个段落：
    full = 拼接所有 run.text
    对全表启用的映射（按原文长度降序）在 full 中查找命中区间
    对每个命中区间 [s, e)：
        定位覆盖该区间的 run 序列
        首个 run：text = 区间前部分 + 代称 + （若区间在本 run 内结束则带后缀部分）
        中间 run：text = ""
        末尾 run（若非首个）：text = 区间后部分
    run 置空后保留（不删除节点，样式不丢失）
```
两个硬性要求：映射按键长度降序执行（防"张三丰"被"张三"先吃掉）；表格单元格、页眉页脚与正文走同一代码路径。

【自查】`tests/unit/test_docx_redactor.py`
- 构造跨 3 个 run 的"张三"（如加粗拆分），替换后只剩"甲某"且原加粗样式保留在首个 run
- 含表格、页眉的样例 docx：替换后 python-docx 重新打开无异常，表格结构不变
- 映射含"张三"与"张三丰"时，"张三丰"被正确替换为对应代称而非"甲某丰"

### 任务 M1-7：批次队列与状态机 `pipeline.py`

实现文件状态：`待扫描 → 扫描中 → 已扫描/扫描失败 → 已脱敏 → 已导出`，附加规则：映射变更使全部"已脱敏"文件回到"已扫描"并置"需重扫"角标；"已导出"仅在"已脱敏"后可达到；失败文件记录原因，不阻塞队列。扫描放 QThread，UI 不阻塞。

【自查】手工走查：导入 5 个文件含 1 个损坏文件 → 4 个正常流转，1 个标错，队列不中断；启停任一映射项后，已脱敏文件出现"需重扫"角标

### 任务 M1-8：主界面与映射面板（ui/ 全部）

按产品方案 4.1 原型实现三栏 + 顶栏：
- 顶栏：模式切换（本阶段回溯模式置灰）、导入文件、导入文件夹、批量导出、设置
- 左栏文件队列：文件名 + 状态图标 + 失败原因悬浮提示
- 中栏预览：docx 转图片或 HTML 渲染（可用 docx→LibreOffice 转 PDF→图片，或直接段落文本渲染，M1 允许文本级预览），脱敏处高亮，"原文|脱敏文"切换
- 右栏映射面板：顶部"添加自定义脱敏项"按钮 + 搜索框；下方类别折叠组（人名/号码/机构/地址/其他）+ "待确认"组（黄色标识，一键采纳/忽略）；每项含 原文→代称、启停开关、出现次数、悬浮显示所在文件列表
- 弹窗：自定义项弹窗（敏感词 + 代称留空自动分配）；关闭软件时存在未导出映射的强提示弹窗；存在待确认项时导出前的确认弹窗

【自查】手工走查清单：
- [ ] 拖入文件夹，全部 docx 入队并自动扫描
- [ ] 关闭某映射项，预览即时恢复原文
- [ ] 新增自定义敏感词，全批次所有文档即时生效
- [ ] 待确认项不采纳时导出，弹确认框
- [ ] 关闭软件且未导出映射，弹强提示

### 任务 M1-9：导出与明文映射 JSON

实现：批量导出到 `脱敏输出/`，重名追加 `_1` 序号；同步生成映射 JSON（结构见第 4 章），M1 阶段明文保存，文件名 `映射_<批次号>.json`。

【自查】M1 阶段验收（全部通过才可进入 M2）：
- [ ] 上述全部 pytest 与手工走查通过
- [ ] 用 3 份真实合同/判决书实测：号码类召回 ≥ 99%（人工核对）
- [ ] 导出 docx 与原文档逐页比对，除替换词外排版零差异
- [ ] 映射 JSON 内容完整、可人工阅读

---

## 4. 映射文件格式定稿（M2 加密前即按此结构）

```json
{
  "version": "1.0",
  "batch_id": "uuid4",
  "created_at": "ISO8601",
  "doc_manifest": [{"file": "合同1.docx", "sha256": "..."}],
  "mappings": [
    {
      "id": 1,
      "category": "person",
      "original": "张三",
      "alias": "甲某",
      "occurrences": 12,
      "enabled": true,
      "source": "rule|lexicon|ner|custom",
      "confidence": 0.99
    }
  ]
}
```

加密外壳（M2 起默认启用）：`{"enc": "AES-256-GCM", "kdf": "scrypt", "salt": "<base64>", "nonce": "<base64>", "data": "<base64密文>"}`。密钥 = scrypt(用户密码, salt, n=2^14, r=8, p=1)，密码不明文落盘。

---

## 5. M2 阶段：PDF、加密与回溯

### 任务 M2-1：PDF 解析与替换 `pdf_parser.py` / `pdf_redactor.py`

解析：`page.get_text("dict")` 取 span 级文本与坐标；无文本层的页面判定为扫描件，整文件标注"暂不支持"。

替换：对每条启用映射 `page.search_for(原文)` 得矩形区域 → `page.add_redact_annot(rect, text=代称)` → `page.apply_redactions()`。字号适配：取原 span 字号为起点，若 `fitz.get_text_length(代称, fontsize)` 超出 rect 宽度则逐级减 0.5 直至适配或到 6pt；仍放不下则记录溢出告警。替换后对每页重取文本验证代称存在。

【自查】
- 样例 PDF 替换后：原敏感词不可选中（已被抹除）、代称可见、版面无错位
- 构造长原文→短代称、短原文→长代称各一例，后者触发字号缩小且无溢出告警误报
- 扫描件 PDF 被正确识别并标注

### 任务 M2-2：映射文件加解密 `crypto.py`

实现 `export_encrypted(mapping, password, path)` 与 `load_encrypted(path, password)`；解密失败抛 `WrongPasswordError`（界面提示重输，不提供找回）。

【自查】pytest：加密→解密往返一致；错误密码抛指定异常；密文文件头不含任何明文映射内容（用二进制查看确认"张三"等字样不存在）

### 任务 M2-3：回溯还原 `restorer.py`

实现：
1. 导入 AI 处理后文档（docx/txt/md）+ 映射 JSON + 密码
2. 校验：解密失败或 JSON 结构非法 → 拒绝；文档名不在 `doc_manifest` → 弹警告但允许继续（AI 产出文档与原文件哈希不同属预期，不做哈希拒绝）
3. 逆替换：按代称长度降序，将文档中所有代称替换回原文（docx 走 M1-6 同一替换器；txt/md 直接字符串替换）
4. 生成"未还原项"报告：映射中启用但未在处理后文档中出现的代称清单（代称、原文、原出现次数），导出界面展示并随还原文件存为 `未还原项.txt`

【自查】
- 端到端：脱敏导出 → 模拟 AI 修改（增删段落、保留代称）→ 还原 → 还原文件与原文档在敏感信息处完全一致
- 删除某代称后还原，该代称出现在未还原项报告
- 错误批次映射文件（结构损坏/密码错误）被拒

### 任务 M2-4：界面收尾

- 顶栏回溯模式启用，切换时清空当前队列并提示
- 导出流程加"设置映射密码"步骤（两次输入校验）
- 回溯模式界面：文档导入区 + 映射文件导入区 + 密码框 + 还原预览 + 未还原项面板

【自查】M2 阶段验收：
- [ ] M2-1 ~ M2-4 全部自查通过
- [ ] 防火墙全阻断环境下完整走通脱敏与回溯（验证离线性）
- [ ] 映射文件脱离软件无法读取内容（加密有效性）

---

## 6. M3 阶段：NER 模型融合与性能优化

### 任务 M3-1：模型选型与转换

- 选型限定可商用许可的中文 NER 模型（决策点：优先蒸馏版 RoBERTa-ner 类，输出 PER/ORG/LOC 三类；选定前向产品负责人确认授权条款）
- 转 ONNX，放入 `app/resources/models/`，onnxruntime CPU 推理
- `ner.py`：文本分句 → tokenize → 推理 → BIO 解码 → 实体片段 + 置信度

### 任务 M3-2：三路融合

engine.py 接入 NER 层：人名/机构/地址类命中与规则、词典层结果按 M1-4 仲裁规则合并；NER 置信度 ≥ 0.90 直接 auto，0.60~0.90 进 pending；NER 与词典层同时命中同一区间时置信度取高者并升一档来源标记。

### 任务 M3-3：评测体系 `tests/eval.py`

- 语料：`tests/corpus/` 放不少于 100 份真实文书（需产品负责人提供），每份配同名标注 JSON（实体、类别、起止位置）
- 评测：跑识别引擎比对标注，输出分类别 P/R/F1
- 上线门槛断言：人名 R≥0.95、P≥0.90，号码类 R≥0.99，未达标脚本退出码非 0
- 回归门槛：每次改动 rules/lexicon/ner 后必须跑 eval.py，指标回退即阻断合入

【自查】eval.py 在语料上跑通并输出指标表；人为删掉一条姓氏词典项，指标下降且断言失败（验证评测有效性）

### 任务 M3-4：性能优化

- 预扫描按文件多进程（ProcessPoolExecutor），NER 模型每进程单例
- 目标：100 页 docx 预扫描 ≤ 10 秒（目标机实测记录）
- 大文件（>500 页）进单独慢速队列并提示

### 任务 M3-5：打包分发 `packaging/build.spec`

- PyInstaller onedir，`--add-data` 打包 lexicons 与 models，关闭控制台窗口
- 产物体积预期 200~300MB，实测记录
- 在干净 Windows 机器（无 Python）上安装实测全部功能

【自查】M3 阶段验收 = 第 1.2 节全部验收指标达成 + 干净机器实测通过

---

## 7. 全局最终自查清单（交付前逐项打勾）

功能完整性：
- [ ] docx、pdf（文字型）批量导入、扫描、脱敏、预览、导出全链路通
- [ ] 映射面板启停、删除、自定义新增、同名拆分全部生效且即时重渲染
- [ ] 待确认组默认不生效，导出前弹确认
- [ ] 脱敏导出 + 加密映射生成；回溯 + 未还原项报告生成
- [ ] 扫描件、加密 PDF、损坏文件、被占用文件均有明确标注且不阻塞队列
- [ ] 关闭软件时未导出映射的强提示生效

质量门槛：
- [ ] eval.py 指标达标（人名 R≥95%/P≥90%，号码类 R≥99%）
- [ ] 抽样 10 份导出文档人工比对，排版零差异
- [ ] 100 页 docx 预扫描 ≤ 10 秒
- [ ] 还原准确率 100%（映射覆盖范围内）

安全与合规：
- [ ] 防火墙全阻断环境功能完整（无隐藏外联）
- [ ] 映射文件加密有效，密码错误被拒，文件无明文残留
- [ ] 软件关闭后内存无映射明文（重启后必须重新导入映射文件）
- [ ] 首次启动界面明示"脱敏效果以人工确认为准"

---

## 8. 异常处理速查表（实现时严格照此）

| 异常 | 处理 |
|---|---|
| 扫描件/图片型 PDF | 队列标注"暂不支持"，附说明，不阻塞 |
| 加密 PDF / 损坏 / 被占用 | 单文件标错注明原因，队列继续 |
| >500 页文件 | 警告 + 慢速队列 |
| 同名不同人 | 右键"拆分主体"，按文件粒度拆代称 |
| AI 改写了代称 | 进"未还原项"报告 |
| 文档名不在 manifest | 警告但允许继续还原 |
| 映射 JSON 损坏 / 密码错误 | 拒绝，提示重输（无找回通道） |
| 替换文本溢出 PDF 原区域 | 自动缩小字号，仍溢出则记录告警 |

---

## 9. 开放问题（实现前需产品负责人确认）

1. "法官/律师等司法人员姓名是否脱敏"开关默认值（当前默认：保留不脱敏，依据公开裁判文书惯例）
2. 号码类默认保留位数是否有行业偏好（当前：身份证/手机号前3后4，银行卡前4后4）
3. 评测语料：需提供不少于 100 份可脱敏使用的真实文书（M3-3 前置）
4. NER 模型选型的商用授权确认（M3-1 前置）
5. 性能目标机型的实测基线（100 页 ≤ 10 秒的校准环境）
6. PDF 预览渲染保真度要求：M1 允许文本级预览，是否需要在 M3 前升级为版式级渲染

---

*文档版本：v1.0（2026-08-31）· 依据产品方案优化版编写 · 修改本文档需同步更新第 1 章定稿约定与第 7 章自查清单*
